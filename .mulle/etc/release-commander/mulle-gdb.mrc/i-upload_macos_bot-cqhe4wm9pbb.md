# Upload macOS bottle to release

Verify that the macOS bottles built by `build-bottle.yml` are present on the
GitHub release. The workflow should have uploaded them automatically.

If bottles are missing, re-trigger the `build-bottle.yml` workflow or upload
manually.

# Copy Formula from homebrew/prerelease to homebrew/software

Copy the `mulle-gdb.rb` formula from the repo root to
`mulle-objc/homebrew-software`. The formula should already have the
`bottle do` block updated with the correct sha256 values from the
bottle build step.
